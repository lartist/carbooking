{% extends 'sidebar_layout.html.twig' %}

{% set pageTitle = 'template.car.new.page_title'|trans %}
{% set h1 = 'template.car.new.h1'|trans %}

{% block content %}

    <div class="card">
        <div class="card-body">
            {{ include('car/_form.html.twig') }}
        </div>
    </div>
{% endblock %}
